# Scripts (optional, not used at runtime)

| Script | Purpose |
|--------|---------|
| `generate_flow_pdf.py` | Regenerate `docs/Voice_Agent_Complete_Flow_Guide.pdf` (needs `pip install reportlab`) |
| `sync_n8n_workflow.py` | Copy `n8n/build_call_log.js` into `n8n/voice_agent_actions.json` Build Call Log node |
